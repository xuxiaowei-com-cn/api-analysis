# Spring 配置
spring:
  # 数据源
  datasource:
    # 数据库驱动
    driver-class-name: com.mysql.cj.jdbc.Driver
    # 数据库用户名
    username: root
    # 数据库密码
    password: root
    # 数据库连接串
    url: jdbc:mysql://127.0.0.1:3306/api-analysis?useSSL=false&serverTimezone=GMT%2B8&characterEncoding=UTF-8
